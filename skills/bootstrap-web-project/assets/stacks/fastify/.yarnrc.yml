packageExtensions:
  # fastify-type-provider-zod declares its optional OpenAPI integrations as
  # required peers. Keep this explicit until upstream marks them optional.
  fastify-type-provider-zod@*:
    peerDependenciesMeta:
      "@fastify/swagger":
        optional: true
      openapi-types:
        optional: true
